"use client";

// import { useBalance } from "@repo/store/balance";

export default function() {
  // const balance = useBalance();
  const balance = 1000;
  return <div>
    hi there {balance}
  </div>
}